<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ファイル登録画面</title>
</head>

<body>
    <h2>現在のファイル一覧</h2>
    <ul>
        <% filenames.map(file=> { %>
            <li>
                <form action="/filedel" method="post">
                    <input type="hidden" name="filename" value="<%= file %>">
                    <p><%= file %></p>
                    <input type="submit" value="削除">
                </form>
                
            </li>
            <% }) %>
    </ul>
    <h2>ファイルを取り込む</h2>
    <form action="/upload" method="post" enctype="multipart/form-data">
        <input type="file" name="image">
        <button type="submit">Upload</button>
    </form>
    <h2>表示設定</h2>
    <form action="/display" method="post">
        <h3>表示方法</h3>
        <select name="displaytype">
            <option value="1" selected>選択してください</option>
            <option value="1">通常表示</option>
            <option value="3">パターン２表示（現状は秒数の倍の時間で切り替え）</option>
        </select>
        <h3>画像切り替え時間(秒)</h3>
        <input type="text" name="time" value="3">
        <button type="submit">表示画面に移動する</button>
    </form>
</body>

</html>